{
    "name": "pdv-master",
    "version": "1.0.0",
    "private": true,
    "description": "PDV Master - Sistema de Ponto de Venda e ERP",
    "scripts": {
        "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
        "dev:backend": "npm run dev --prefix backend",
        "dev:frontend": "npm run dev --prefix frontend",
        "build:backend": "npm run build --prefix backend",
        "build:frontend": "npm run build --prefix frontend",
        "docker:up": "docker-compose up --build",
        "docker:down": "docker-compose down",
        "docker:prod": "docker-compose -f docker-compose.prod.yml up --build -d",
        "test:all": "npm run test --prefix backend && npm run test --prefix frontend",
        "lint:all": "npm run lint --prefix backend && npm run lint --prefix frontend",
        "sync": "powershell -ExecutionPolicy Bypass -File ./sync-project.ps1"
    },
    "workspaces": [
        "backend",
        "frontend"
    ],
    "devDependencies": {
        "supabase": "^2.70.5"
    }
}